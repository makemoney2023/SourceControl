# Legacy flat screens (v1)

Archived reference UI from the first Org Command Center pass. The default app shell is `src/jarvis/JarvisShell.tsx` (Jarvis 3D theater).

These modules are not linked from `App.tsx`.
