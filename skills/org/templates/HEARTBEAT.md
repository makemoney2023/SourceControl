# HEARTBEAT — on-wake checklist

Copy to `skills/org/positions/<slug>/HEARTBEAT.md` and tailor. Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm your position slug, reporting line, and `llm_tier` from the manager packet.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints. Do not invent packs.
3. **Leases** — Only write paths in `write_lease`. Give each IC a non-colliding subset.
4. **Delegates** — Spawn only allowed ICs from your position SKILL.md; each IC packet must include `llm_tier`.
5. **Handoffs** — Await IC handoffs under `docs/projects/<active>/business-idea/HANDOFFS/`; merge; write manager brief.
6. **Escalate** — Use ESCALATION tags when blocked; do not mark the phase complete.
7. **Exit** — Leave durable next action in the manager brief; stop cleanly.
