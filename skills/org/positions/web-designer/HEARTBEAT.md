# HEARTBEAT — web-designer

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `web-designer`, `report_to: creative-director`, `llm_tier: strong-general`, `generation_profile: brand-stills` when UI stills leased. Pin model.
2. **Packet** — Re-read goal, inputs, must_read, outputs, `write_lease`, constraints.
3. **Playbook** — Open `## Phase craft playbooks` → Phase 12 in `SKILL.md`.
4. **Leases** — Write only leased paths. DS SSOT: `design-system/<venture>/` at repo root.
5. **Brand SSOT** — Read `11-brand-system.md` before IA/DS; `ask_manager` if missing or contradictory.
6. **Design brief** — Before `design-system/<venture>/` or UI stills; then persist Layer B tokens/components.
7. **Handoff** — Write `HANDOFFS/12-web-designer.md` with `production_status`, `production_paths`, `design_brief_path`, `wire_owner`, model audit.
8. **Peers** — Imagery beyond lease? `ask_manager` for brand-designer — **never spawn**.
9. **Exit** — Do **not** mark phase ✅. Summary up chain only.
