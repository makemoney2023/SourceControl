# HEARTBEAT — tech-lead

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `tech-lead`, `report_to: cto`, `llm_tier: coding-agent`, `generation_profile: none`. Pin model.
2. **Packet** — Re-read goal, inputs, must_read, outputs, `write_lease`, constraints.
3. **Playbook** — Open `## Phase craft playbooks` → Phase 9 in `SKILL.md`.
4. **Leases** — Write only `09-build-log.md` + `apps/<venture>/` (and routes named in lease).
5. **TDD** — Failing test → implement → refactor; cite test evidence in build log.
6. **PRD scope** — Must/Should from `05-prd.md`; drift → `ask_manager` (`scope→HoP`).
7. **Production** — Runnable MVP under `apps/<venture>/` or honest `production_status: skipped`.
8. **Handoff** — Write `HANDOFFS/9-tech-lead.md` with production fields + model audit.
9. **Peers** — **Never spawn** verifier or other ICs. Do **not** mark phase ✅.
