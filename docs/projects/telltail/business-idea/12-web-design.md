---
venture: telltail
org: Velocity Agency
phase: "12"
title: Web design — IA + outline
owner: creative-director
reports_to: ceo-strategist
status: CD-merged — Phase 12 not marked complete
ic: web-designer
date: 2026-08-21
llm_tier: strong-general
llm_model: composer-2.5
generation_profile: brand-stills
generation_used: none
fallback_applied: false
production_status: skipped
skip_reason: explore · outlines only · no store
design_brief_path: docs/projects/telltail/business-idea/12-web-design.md#design-brief-required-before-production
photoreal_qa: ""
wire_owner: none
4B: closed
founder_lock: product-surface-one-thread
---

# 12 — Web design — Telltail

**Mode:** explore · **4B:** closed · **Not** Blacksage · **Not** Sieger · No live site · No store · No paid · Outlines only

Label key: **[F]** fact/lock · **[I]** inference · **[A]** assumption / this-pass craft

Canonical disk: `/Users/cbsuperpatch/Desktop/ClaudeSkills/docs/projects/telltail/business-idea/` (local only, no OneDrive / iCloud / Google Drive).

This file is **Layer A** IA + token mapping, **CD-merged**. Layer B (`design-system/telltail/` at repo root **or** under `business-idea/`) is **not** created this pass. UI stills are **not** rendered. img2threejs is **not** run. Phase 12 is **not** marked complete.

**Holding line (do not rewrite):** *See the signal. Do the next right thing — and know when to stop.* **[F]** `03-strategy.md` / Phase 11

Tokens inherit Phase 11 SSOT. Roles must not invert. **[F]** `11-brand-system.md`

**Founder lock (2026-08-21, supersedes the first draft):** **Product surface = one thread.** Capture, refuse-first, moment card, and paywall-after-Lite-complete are messages/cards **in that thread**. Chat is not a side lane next to a capture dashboard. Not a curriculum dashboard. Not a card grid of lessons. Not a sticker translator. Not a Pupford-style lesson map. **[F]** CD / founder · tightens US-21

---

## Summary

Telltail’s **marketing** site is outline-only: Home / How it works / Pricing, plus two later pages that are **not** home (`/vs-dog-translator`, `/science`). The **product** is a chat UI. The owner describes the scare and attaches a clip or stills. A vision card appears only after media. Refuse, next-right-thing, and when-to-stop live **in the thread**. **[F]** PRD areas A–I mapped into the thread · GTM CTAs · founder lock

This pass does **not** invent a sitemap and does **not** invent a lesson map. Functional areas stay A–I; they are **message types**, not tabs. **[F]**

Layer B is **skipped.** Reason: explore · outlines only · no store. **[F]** CD packet. `generation_used: none`. `photoreal_qa: ""`. `wire_owner: none`. No empty `design-system/telltail/` folder was created (repo root or under `business-idea/`). No UI stills. No 3D dog. No live URL.

Kids leftover is a **product refuse** (a card in the thread), not a family-exclusion site. Household may appear. Child-as-coaching-subject does not. **[F]** Phase 10 / 11

A5 named training voice stays **OPEN**. No Cesar. No PetGPT avatar. **[F]**

---

## Route map

Derived. Do not add product areas. **[F]** PRD functional areas A–I + GTM demand path + founder thread lock.

### Marketing (outline only — no live site)

| Route | Job | v1 nav? | Source |
|-------|-----|---------|--------|
| `/` Home | Photoreal kitchen / doorway hero. Holding line. CTA: **Film this moment / Finish one real scare** (opens the thread). Anti-CTA: “See what your dog is thinking.” Footer disambiguates Telltail Dog Training (Little Rock). Do not wear `telltail.com`. | Yes | **[F]** PRD IA · GTM hero · founder lock |
| `/how-it-works` | One scare in a thread: tell it → attach → card **or** refuse. Not a 6-week syllabus. | Yes | **[F]** founder lock (marketing trio) |
| `/pricing` | Harm-per-wrong-fire. `$12/mo` / `$99/yr` · 60 Flash + credits. Never `$9.99`. Never unlimited. Never “what serious apps do.” App paywall still only **after** a completed Lite scare (US-07). | Yes | **[F]** US-08/09 · GTM · founder lock |
| `/vs-dog-translator` | One anti-toy harvest page. **Never the homepage.** | No — later (week-10 harvest) | **[I]** GTM |
| `/science` | Honesty note (Sci Rep 2025 — we did not solve LVLM chance; no accuracy %). | No — later, not this pass’s home | **[I]** GTM weeks 9–12 |
| Body-language-as-training hub (`/dog-body-language` + spokes) | Content pillar, not v1 chrome. | No — later | **[F]** GTM pillars; **[A]** URLs from GTM hub/spoke |
| Store listing (if it ever exists) | Lifestyle + Education. Get — 60 honest reads disclosed. | n/a this pass | **[F]** |

**Not marketing this pass:** curriculum hub, trainer-seat SKU, Entertainment category, translator homepage, daily-scan streak, paid landing for Traini keywords, Pupford-style lesson catalog.

### App (one thread — map PRD areas → message/card types)

**Product chrome is the thread.** One primary route. Areas A–I are **not** a tab bar and **not** a dashboard. **[F]** founder lock · US-21

| Area | In-thread object | Job | Priority |
|------|------------------|-----|----------|
| **I. Chat (the chrome)** | `/` in-app = the thread. Composer: text + attach. | Owner describes the scare. Text is context. Media in thread unlocks a vision card. Text-only ≠ “I saw your dog.” | Must — **this is the product** |
| **A. Capture & media** | Attach / camera sheet **from the composer** | Live clip or just-shot upload. Cloud-disclosure before send (US-11). Camera-denied → Photos (US-14). Not a capture dashboard. | Must |
| **B. Read** | In-thread progress message | One cloud call. Plus = Flash-class. Lite = cheap-model. Never a custom detector. Never “on-device.” | Must |
| **C. Refuse-first gate** | Gate **before** any moment-card chrome. Lands as **Refuse card in the thread**. | Bite-risk · kids-in-frame · low-confidence. Quota cannot skip. | Must |
| **D. Moment card** | **Moment card in the thread** | Signals + confidence + 1–3 next-60s actions + stop-rule. Only after C passes. Next-right-thing and when-to-stop live here. | Must |
| **E. Claims & escalate** | Copy on the in-thread cards / marketing `/pricing` / later listing — not a tab | Banned tokens never ship. Escalate-don’t-diagnose. A5 unnamed. | Must |
| **F. Lite explore** | Same thread loop | 3–5 cheap-model reads. First scare must complete (card or refuse in-thread). | Must |
| **G. Plus / paywall / credits** | **Paywall card in the thread** only **after** a completed Lite scare (US-07). Credits = overflow after 60, not a third column. | `$12` / `$99` · 60 + refuse-cannot-skip + no unlimited | Must *of the test* |
| **H. History** | Prior **threads** (scrollback or a thread list). Not a lesson grid. | Prior reads / moment log. Kids-in-frame frames are not the default retained object. | Should |
| **Later / out** | Do not add chrome | Curriculum (US-17), trainer seat (US-18), Android (US-19), HITL (US-20), Pupford lesson map | Out |

**Loop (do not reorder):** owner text (optional) → A attach → B → C → **D or refuse in the thread** → (Lite complete) G as a card in the same thread. **[F]** US-01–07, US-21, founder lock

No curriculum-first nav. No trainer-seat SKU. No card-grid of lessons. No capture dashboard beside chat. **[F]**

Optional deep link later: a message id inside the thread. Do not promote `/card/:id` or `/refuse/:id` or `/capture` as primary destinations.

---

## Page templates

Outline chrome only. No Figma, no stills. **[A]** layout craft on locked IA.

### T-Home (marketing)

1. Wordmark (Newsreader, Ink) + competence lockup (Plex Sans, Slate): `Dog training` or `Body language · next step`. Never `translator`.
2. Holding line in Display.
3. Hero still slot (later H1/H2 from Phase 11 brief) — kitchen or doorway. CTA-safe right/bottom third.
4. Primary CTA: **Film this moment** (Sign fill, Paper type) — opens the thread with attach. Secondary: **Finish one real scare**.
5. Proof band (below the fold — see next section).
6. Nav: Home · How it works · Pricing. **[F]** founder lock
7. Footer: not Telltail Dog Training (Elizabeth Silverstein, Little Rock). We do not own `telltail.com`. Cloud: video leaves the phone.

### T-How-it-works (marketing)

1. Three beats, not a syllabus: **Tell the scare** → **Attach a clip** → **Get a card or a refuse in the thread**.
2. Show a quiet thread mock (later still), not a lesson map.
3. Anti: 6-week plan, sticker quote, “see what your dog is thinking.”

### T-Pricing (marketing)

Same disclose as the in-thread paywall card. `$12/mo` / `$99/yr` · 60 + refuse-cannot-skip + no unlimited. CTA still **Film this moment** for unpaid visitors (do not paywall-before-card). **Get Plus — $12/mo** is copy on this page; the app still waits for US-07.

### T-Thread (product chrome — primary)

1. Field ground. One composer at the bottom: text + attach (camera / Photos). Cloud-disclosure on attach, before send (US-11).
2. Empty state: “What just happened?” — invite the scare story. Attach to unlock a vision card. No “I already saw your dog.”
3. Owner messages: Plex Sans, Ink.
4. In-thread objects only: progress · **Refuse card** · **Moment card** · **Paywall card** (after Lite complete) · **Zero-remaining card**.
5. No ghost trainer avatar (A5 OPEN). No lesson rail. No dashboard hamburger of modules.
6. Safe-area insets. One-hand kitchen-hour. Composer stays visible.

### T-Composer-attach (was T-Capture)

Not a route. A sheet from the composer.

1. Camera or Photos.
2. Cloud-disclosure line (US-11) before send.
3. Optional context chips in the same composer (kids / food / doorbell / visitor).
4. Confirm send into the thread. No “I already saw your dog” from chips alone.

### T-Read (in-thread progress)

1. Quiet Paper bubble. Mono meter hint optional (Lite remaining / Plus remaining).
2. No fake result. No green “analyzing safely” chip.

### T-Moment (in-thread card)

See motifs. First-class **in the thread**, not a toast, not a separate course screen.

### T-Refuse (in-thread card)

See motifs. First-class **in the thread**. Quota cannot restyle this as an upsell.

### T-Paywall (in-thread card; marketing `/pricing` mirrors it)

1. Eyebrow: Plus · 60 honest Flash reads / month. **[F]** GTM
2. Headline: *Sixty honest reads. A hard stop when the next right thing is to stop.* (or the holding line). **[F]** US-09
3. Subhead: we cap Flash so a wrong trainer cannot fire two hundred times a month. A refuse still happens at zero credits.
4. Price: **$12/month** or **$99/year** — same 60-read meter. Disclose 60 + refuse-cannot-skip + no unlimited **in the same viewport as price**.
5. Primary: **Get Plus — $12/mo**. Secondary: **Stay on Lite**.
6. Credits are **not** a hero column. Trainer seat is off the grid.
7. Kill if K1: Plus is not a product. Do not paper with accuracy % or more credits.
8. **In-app:** this card appears in the thread only after a completed Lite scare (card or refuse). **[F]** US-07

### T-Zero-remaining (in-thread card)

Credits or wait. Refuse still on. **[F]** GTM

### T-History (Should)

Prior **threads**, or long scrollback. Not a card grid of lessons. No child-frame thumbs as default. No trainer-share portal this pass.

---

## Hero / proof band model

**Hero job:** get one real scare into the thread (told + attached). Not a translator demo. Not a lesson catalog. **[F]**

| Band | What sits there | What does not |
|------|-----------------|---------------|
| **Hero** | Photoreal kitchen / doorway / visitor (Phase 11 H1/H2 when rendered). Wordmark + holding line. Sign CTA into the thread. | Traini quote sticker. Speech-bubble dog. Cesar / PetGPT face. Green safety light. 3D dog. Lesson map. |
| **Proof** | Same-screen claims rule in caption: observable signals + “likely”; stop-rule; “Not a diagnosis. Not a safety clearance.” Cloud honesty. A thread-shaped still later (card **in** chat). | Accuracy % · vs-vet · Como 78% · Dogs Trust 80/24 as US fact · named voice (A5 OPEN) · “relaxed / safe / won’t bite” |
| **Neighbor (optional, later)** | Pupford / Zigzag as *week vs minute* — never as stickers on the hero, never as our primary UI. | Traini in the hero or screenshot. Scan-count cage match with Tailo. Copying their lesson map. |

Proof is **competence + refuse**, not social proof we do not have (0 interviews this pass; do not invent). **[F]**

When stills exist, they come from `11-brand/design/telltail-design-brief.md` — not a second prompt bank. **[F]**

---

## CTA hierarchy

**[F]** GTM surface CTAs. Do not invent a fifth primary. Hero CTA opens the **thread**.

| Priority | Surface | CTA | Anti-CTA |
|----------|---------|-----|----------|
| 1 | Hero / How it works | Film this moment / Finish one real scare | “See what your dog is thinking” / “Talk to your dog” / “Browse lessons” |
| 2 | Lite complete (in-thread) | Continue with Plus — 60 honest reads | “Unlock unlimited AI” / paywall *before* the card |
| 3 | Paywall primary (in-thread + `/pricing`) | Get Plus — $12/mo | “Compare scan counts” / “Same as Tailo” |
| 4 | Paywall secondary | Stay on Lite | Guilt about leftover credits |
| 5 | Refuse (in-thread) | Stop. Create space. Call a vet / credentialed trainer. | “You’re safe.” “Won’t bite.” “Try anyway.” |
| 6 | Zero remaining (in-thread) | Credits or wait — refuse still on | “Skip this refuse” |

Store (later): Lifestyle + Education; Get — 60 honest reads disclosed. **[F]**

---

## Token → shadcn / Tailwind mapping

Inherit Phase 11 hex and **roles**. Hex may tighten in Figma later; roles must not invert. **[F]** tokens · **[A]** CSS names this pass.

Do **not** persist a `design-system/telltail/` folder this pass. Map below is the brief a later pass copies into repo-root `design-system/telltail/` (org SSOT — not only under `business-idea/`).

### Primitive (raw)

| Token | Hex | Role |
|-------|-----|------|
| Ink | `#1A1814` | Wordmark, body, marks |
| Paper | `#F6F2E9` | App chrome, marketing ground, card field |
| Sign | `#B5522A` | Primary CTA / telltale tick — **not** safety green, **not** kennel gold |
| Refuse | `#6B2C28` | Refuse card title rule / stop — **not** siren red, **not** “all clear” |
| Slate | `#5A564E` | Secondary type, confidence words |
| Field | `#EFE9DC` | **Thread** ground, inset wells, disabled fills |
| Hairline | `#DCD6C8` | Card edges, meter track |
| Quiet | `#8E877A` | Tertiary / placeholder |

### Semantic → shadcn (do not invert)

shadcn `primary` is the **action** color. That is **Sign**, never green, never Ink-as-button-on-Ink.

| shadcn / CSS var | Maps to | Notes |
|------------------|---------|-------|
| `--background` / `--card` | Paper | Warm page, not clinic white |
| `--foreground` / `--card-foreground` | Ink | Body + titles |
| `--primary` / `--primary-foreground` | Sign / Paper | CTA fill. Large type only. Do **not** set 14px body in Sign. |
| `--secondary` | Field / Ink | Quiet wells, Stay on Lite |
| `--muted` / `--muted-foreground` | Field / Quiet | Placeholders |
| `--accent` | Sign at hairline weight | Telltale tick / 2px rule — not a wash |
| `--destructive` | Refuse | **Stop / refuse surfaces only.** Do not use for generic form-delete if that would train “red = error toast.” Refuse is a first-class **in-thread** card, not a toast. |
| `--border` / `--input` | Hairline | |
| `--ring` | Sign | `:focus-visible` ring. Never outline-none without replacement. |
| `--color-success` / green scale | **Do not add** | No safety-green token. No “all clear” chip. |

Tailwind v4 `@theme` (later Layer B — not written to disk now):

```css
@import "tailwindcss";
@theme {
  --color-ink: #1A1814;
  --color-paper: #F6F2E9;
  --color-sign: #B5522A;
  --color-refuse: #6B2C28;
  --color-slate: #5A564E;
  --color-field: #EFE9DC;
  --color-hairline: #DCD6C8;
  --color-quiet: #8E877A;
  --color-background: var(--color-paper);
  --color-foreground: var(--color-ink);
  --color-primary: var(--color-sign);
  --color-primary-foreground: var(--color-paper);
  --color-destructive: var(--color-refuse);
  --color-border: var(--color-hairline);
  --font-display: "Newsreader", "Iowan Old Style", Georgia, serif;
  --font-sans: "IBM Plex Sans", "SF Pro", system-ui, sans-serif;
  --font-mono: "IBM Plex Mono", "SF Mono", ui-monospace, monospace;
  --radius-lg: 16px; /* moment / refuse card — quiet, not pill-toy */
}
```

**Type roles [A]** (Phase 11 stack): Display = Newsreader (holding line, refuse title, wordmark). UI = IBM Plex Sans (actions, chat). Mono = IBM Plex Mono (60, confidence, timestamps). Caption ≥12px. Body ≥16px. `font-variant-numeric: tabular-nums` on the 60 meter. **[I]** web-design-guidelines

**Radius:** 16–20px on moment/refuse cards. Not a 9999 pill. **[F]** Phase 11 motifs

**Motion (later):** 180–240ms ease, transform/opacity only, honor `prefers-reduced-motion`. No bounce, no sticker pop. **[A]** Phase 11 brief · **[F]** guidelines

---

## Design brief (required before production)

Write this before any `design-system/telltail/` files, UI stills, or app chrome. Later persist at **repo root** `design-system/telltail/` (org pack SSOT). This pass skips both that folder and `business-idea/design-system/telltail/`. Do not mkdir empty files and call it complete.

### Packs cited (read before pixels)

| Pack | Why it is in this brief |
|------|-------------------------|
| `skills/community/ui-ux-pro-max-skill/design-system/` | Primitive → semantic → component. No raw hex in components once Layer B exists. |
| `skills/community/openmontage/.agents/skills/tailwind-design-system/` | Tailwind v4 `@theme` — Sign is `--color-primary`, not a green scale. |
| `skills/community/openmontage/.agents/skills/web-design-guidelines/` | Focus-visible, labels, reduced motion, no zoom-lock, semantic buttons. |
| `skills/org/packs/production-artifacts/` | Craft → Design → Production → Wire. Production skipped this pass. |
| `docs/projects/telltail/business-idea/11-brand-system.md` | Token + motif SSOT. Do not invent a second palette. |
| `docs/projects/telltail/business-idea/11-brand/design/telltail-design-brief.md` | FLUX-positive heroes if/when stills are leased. |

### Look & feel

Photoreal household rooms; the **product** is one thread — the sign you notice and the card in that thread that tells you the next minute, or tells you to stop. Paper chrome, Field thread, Ink type, Sign CTA, oxblood refuse. Dry. Quiet. A little literary. Never cartoon empathy. Never a green safety light. Never a lesson dashboard.

| Token | Value |
|-------|-------|
| Ground | Paper `#F6F2E9` (marketing + chrome) · Field `#EFE9DC` (thread) |
| Ink | `#1A1814` |
| Telltale / CTA | Sign `#B5522A` |
| Stop | Refuse `#6B2C28` on refuse surfaces only |
| Type | Newsreader + IBM Plex Sans + IBM Plex Mono |
| Light | Kitchen / doorway. One key. No gel wash. |
| Motion | 180–240ms ease; `prefers-reduced-motion` |
| Mobile | Safe-area insets. Composer docked. Body ≥16px. Do not disable pinch-zoom. |

**Kids:** Household *context* may exist. Do not design a “no kids ever” marketing site. Do not hero a child as the coaching subject. Kids-in-frame is a product refuse (in-thread Refuse card), not a family-exclusion look. **[F]**

**Store (if it ever exists):** Lifestyle + Education. Never Entertainment.

**Price (if it appears):** `$12/mo` / `$99/yr` · 60 Flash + credits. Never `$9.99`. Never unlimited.

**A5:** No named trainer. No Cesar. No PetGPT avatar.

### Component scope (what Layer B must ship later)

1. **Thread** — the product shell. Field ground. Composer + attach. Message list. This is not optional chrome around a dashboard.
2. **Button** — primary = Sign fill / Paper label (“Film this moment”, “Get Plus — $12/mo”). Secondary = Hairline / Ink (“Stay on Lite”). Refuse actions stay on the refuse card, not a Sign button that says “Try anyway.”
3. **Moment card** — first-class **in the thread**. See motifs.
4. **Refuse card** — first-class **in the thread**. See motifs. Not `Toast`.
5. **No quote sticker** — do not add the component.
6. **No lesson map / card grid / curriculum dashboard** — do not add the component.
7. **Meter** — Mono “60” / remaining. Hairline track. Words for confidence (low / med / high or a number in Slate). **No green-to-red bar.**
8. **Paywall card** — T-Paywall in-thread after US-07. Credits overflow after 60, not a third plan column.
9. **Wordmark** — Newsreader + Sign hairline tick. Not a tail. Not a speech bubble.

### Component tokens (when Layer B exists)

| Component | Token use |
|-----------|-----------|
| `Thread` | Field ground; Paper composer; Hairline rules |
| `Button` default | `--button-bg: var(--color-sign)`; `--button-fg: var(--color-paper)` |
| `Button` secondary | Field / Ink / Hairline |
| `MomentCard` | Paper field, Hairline border, radius 16–20, Sign 8×8 tick or 2px rule — **in-thread** |
| `RefuseCard` | Paper or Field; Refuse title rule; body stays Ink — **in-thread** |
| `Confidence` | Slate + Mono — **not** `--color-success` |
| `Meter60` | Mono Ink on Paper; Hairline track |

### A11y targets (lock for later build)

- Ink on Paper for all instructional copy (AA+). **[F]** Phase 11
- Sign on Paper: large / CTA only.
- Refuse on Paper: title + stop-rule. Body on a refuse card stays Ink.
- Never encode safety as green. Confidence is language, not a traffic-light bar. **[F]**
- Icon-only controls (`aria-label`). Form controls labeled. Buttons are `<button>`; nav is `<a>`.
- Visible `:focus-visible` ring in Sign. No `outline-none` without replacement.
- `prefers-reduced-motion`. Animate transform/opacity only.
- Images: explicit width/height; meaningful `alt` (observable scene, not “happy safe dog”).
- Refuse / card results: `aria-live="polite"` when the read lands in the thread — not a silent swap.
- Do not `user-scalable=no`. Do not block paste on fields.
- Thread empty state is a prompt to tell the scare + attach — not a broken feed.
- Composer is a labeled text field + labeled attach control. Do not make attach icon-only without `aria-label`.

### Generation prompts (UI stills — not run)

If a later lease renders UI stills, use Phase 11 H3 (visible refuse on **device**, now clearly **in a thread**) and H1/H2 for marketing heroes. Do not start a second FLUX bank. `generation_used: none` this pass. Cursor gen is draft-only.

---

## Component motifs

Brand objects Phase 11 locked. Web draws them **in the thread**; it does not restyle them into toasts or a lesson grid. **[F]**

### Moment card

- Lives **in the thread** after a passing gate. Paper field, Hairline edge, 16–20px radius.
- Stack: **observable signals** (Plex Sans) → **confidence** (Mono, Slate; low/med/high or a number — never a green bar) → **1–3 next-60s actions** (numbered, verb-led) → **stop-rule** (Display or Plex medium, Ink).
- One Sign tick (8×8 or a 2px rule). Not a dog sticker.
- No quote. No “what the dog is thinking.” No 6-week plan.
- Same-screen claims: “likely,” never “is”; “Not a diagnosis. Not a safety clearance. Does not replace a veterinarian or trainer.” **[F]** GTM

### Refuse card

- **First-class in the thread**, not a toast under a coaching card, not a separate “we won’t help” page. **[F]** US-03 · founder lock
- Paper or Field; Refuse as the title rule / “Stop” / “We will not coach this clip.”
- Body stays Ink. Escalate in Slate: space now; vet / DACVB / credentialed trainer. Not a diagnosis.
- Kids-in-frame: same card. No child identity. No retained kid frame as the hero still. **[F]** US-04
- Quota cannot skip. Do not style as a paywall upsell.
- Auto-refuse inputs **[F]** AC-04.1: kids-in-frame · snap / bite-risk · medical · confidence-floor fail. **Not** auto-refuse: freeze / whale-eye / stare (gate *inputs*). Do not design a “freeze = always refuse” chip.

### No quote sticker

There is no dog-quote component. If a screenshot would read as Traini, it failed. **[F]**

### Thread (the product)

Composer + attach. Vision card only after media. No “I saw your dog” from text. No trainer avatar. No lesson rail. **[F]** US-21 · founder lock

### Paywall

Hero is harm-per-wrong-fire + holding line. 60 + refuse-cannot-skip + no unlimited. SKU `$12` / `$99`. In-app: a card **in the thread** after Lite complete. Marketing `/pricing` mirrors the disclose. **[F]** US-09

---

## A11y notes

| Rule | Why |
|------|-----|
| Ink on Paper for instruction | AA body; Phase 11 lock **[F]** |
| No green-to-red confidence bar | Safety is not a traffic light **[F]** |
| Refuse is an in-thread first-class card, not a 4-second toast | Screen readers + quota honesty **[F]** US-03 |
| Focus-visible Sign ring | Guidelines; kitchen-hour one-hand use **[I]** |
| Do not disable zoom | Guidelines anti-pattern |
| Live region when a card/refuse lands in the thread | Async result **[I]** guidelines |
| Alt text describes the room + weight-shift, not “safe dog” | Claims ladder **[F]** |
| Household may be shown; child-as-subject is refuse, not a hidden family | Kids leftover **[F]** |
| Composer + attach are labeled | Thread is the only product surface **[F]** founder lock |

---

## Anti-patterns

| Ban | Why | Label |
|-----|-----|-------|
| Curriculum dashboard / card grid of lessons / Pupford-style lesson map as primary UI | Founder lock — product is one thread | **[F]** |
| Capture dashboard beside chat / chat as a “side lane” | US-21 tightened — thread **is** the chrome | **[F]** |
| Traini sticker / quote screenshot as the hero | Toy pole | **[F]** |
| Cesar / PetGPT avatar | A5 OPEN | **[F]** |
| “Relaxed / safe / won’t bite” / green safety light | Material safety claim | **[F]** |
| Translator homepage / Entertainment category | Category we refuse | **[F]** |
| Unlimited / `$9.99` / “what serious apps do” | Meter + SKU locks | **[F]** |
| Paywall before the first Lite card-or-refuse | US-07 | **[F]** |
| Text-only “I saw your dog” | US-21 | **[F]** |
| Curriculum-first nav / trainer-seat SKU | Later / out | **[F]** |
| `/vs-dog-translator` as home | Harvest only | **[I]** GTM |
| Body-language hub in v1 nav | Later pillar | **[F]** |
| Dogs Trust 80/24 as US fact | A10 | **[F]** |
| Buying or wearing `telltail.com` / impersonating Telltail Dog Training (Little Rock) | Name collision | **[F]** |
| Blacksage kennel or Sieger show chrome | Wrong venture | **[F]** |
| “No kids ever” family-exclusion site | Product refuse ≠ brand stall | **[F]** |
| Empty `design-system/telltail/` marked complete | Honest skip | **[F]** packet |
| 3D dog / mascot / talking-tail / img2threejs without a product ref | Phase 11 assets empty; no 3D dog | **[F]** / **[A]** |
| Cursor gen as Layer B | Draft only | **[F]** |
| Claiming Phase 9 is open / this file is a live site | 09 is a scoped eval; no wire | **[F]** |
| Invented interviews, TAM, named voice, live URL | Do not invent | **[F]** |

---

## Production status

| Field | Value |
|-------|-------|
| production_status | **skipped** |
| skip_reason | explore · outlines only · no store |
| production_paths | none — did **not** mkdir `design-system/telltail/` (repo root or `business-idea/`) |
| design_brief_path | this file, `## Design brief (required before production)` |
| generation_used | none |
| photoreal_qa | *(empty — no UI stills)* |
| wire_owner | none |
| 3D / img2threejs | **skipped** — no product ref on disk (Phase 11 `11-brand/assets/` empty); no 3D dog; no talking-tail |

Do **not** treat this markdown as a shipped design system. Do **not** complete production with empty files or Cursor GenerateImage.

---

## Fact / inference / assumption (this file)

| # | Statement | Label |
|---|-----------|-------|
| 1 | Product chrome = one thread; PRD A–I are in-thread objects, not tabs | **[F]** founder lock · US-21 |
| 2 | Marketing v1 nav = Home / How it works / Pricing | **[F]** founder lock |
| 3 | Tokens + motifs inherited from Phase 11; Sign ≠ green | **[F]** |
| 4 | CTAs from GTM; hero opens the thread | **[F]** |
| 5 | `/vs-dog-translator` is a later harvest, not home | **[I]** GTM |
| 6 | `/science` later honesty page, not this pass’s home | **[I]** GTM |
| 7 | shadcn/Tailwind names and template chrome | **[A]** this-pass mapping |
| 8 | Layer B skipped: explore · outlines only · no store | **[F]** packet |
| 9 | Interviews, TAM, named voice (A5), live URL | **not invented** |

---

## Eng handoff (later Phase 9 — not open)

Phase 9 already ran a **scoped kid-vs-dog eval** (`09-build-log.md`). That is **not** a Next.js MVP and is **not** re-opened by this file. When a future Phase 9 is dispatched with `apps/telltail/` leased:

1. Consume **this file** + `11-brand-system.md`. Do not invent a second palette.
2. Persist tokens at repo-root `design-system/telltail/` from the Design brief section (primitive → semantic → component). Do not treat `apps/telltail/design-system/` as SSOT.
3. **Ship one thread as the app.** Map A–G to in-thread message/card types. Do not build `/capture`, `/card/:id`, `/refuse/:id` as the primary IA. Paywall card only after US-07.
4. shadcn: remap `primary` → Sign, never default green. Do not add a success token for confidence.
5. Moment + refuse are **in-thread cards**, not `toast`, not a course screen.
6. Attach-in-composer is required for a vision card (US-21). Text-only never claims vision.
7. Cloud-disclosure copy before upload (US-11).
8. Fonts: Newsreader + IBM Plex Sans + IBM Plex Mono (`font-display: swap`).
9. A11y targets in the Design brief are acceptance, not polish.
10. Do not run img2threejs on a dog. Phone still-life only if a real product ref exists.
11. No live domain. `wire_owner` stays none until an operator names hosting.
12. Do not implement a Pupford-style lesson map, curriculum dashboard, or card grid of lessons.

**Not claiming:** Phase 9 open, a Vercel project, a Figma file, or a store listing.

---

## Downstream

| Seat / phase | Inherits |
|--------------|----------|
| **Creative Director** | Merge + manager brief. I do not write `HANDOFFS/12-manager-creative-director.md`. Do not mark Phase 12 complete. |
| **Later web-designer / brand-designer** | Persist DS from the embedded brief; stills from Phase 11 FLUX bank only. Thread-shaped UI still if leased. |
| **Later Phase 9 tech-lead** | **One thread** + token map + a11y. Phase 9 is not open now. |
| **Phase 13 copy** | CTA strings locked; do not rewrite the holding line. Thread empty-state = scare story, not “browse lessons.” |
| **Phase 14 pages** | Home / How it works / Pricing / harvest / science stay in this IA. Do not promote harvest to home. |
| **Verifier** | Confirm honest skip (no DS folder, no stills, brief present, Sign not green, no lesson-map IA). |

---


---

## IC merge

| IC | Path | Verdict | Notes |
|----|------|---------|-------|
| `web-designer` | `12-web-design.md` · `HANDOFFS/12-web-designer.md` | ready_to_merge → **merged** | First draft had sibling routes; superseded on disk to thread-as-chrome. Tokens + skip kept. Brand-designer not spawned. |

**CD accept:** Product chrome is the thread. Marketing = Home / How it works / Pricing. `/capture`, `/card/:id`, `/refuse/:id` are not primary destinations. Sign stays `#B5522A`. Honest Layer B skip. Holding line unchanged.

## NOT doing

- Layer B `design-system/telltail/` (either path)
- UI stills / Cursor gen as a complete
- img2threejs / 3D dog
- A live site or `wire_owner` other than none
- A dashboard / lesson map / card-grid primary UI
- Interviews, TAM, named voice, live URL
- Family-exclusion marketing as a kids leftover
- Blacksage / Sieger look
- Writing the manager brief
- Marking Phase 12 complete
